# Server Configuration
PORT=4000                               # Port for backend server
MONGO_URI=mongodb://mongodb:27017/github-migrations  # MongoDB connection URI (using service name with Docker)
NODE_ENV=development                    # Environment: development or production

# GitHub Configuration (used by server for API calls)
GITHUB_TOKEN=your-github-token-here     # Personal Access Token with repo, read:org, read:enterprise scopes
GITHUB_ENTERPRISE_NAME=your-enterprise-slug  # Your GitHub Enterprise name/slug

# Admin Authentication (IMPORTANT: change these in production!)
MARS_ADMIN=admin                        # Username for Settings page access
MARS_PASSWORD=secure123                 # Password for Settings page access

# Optional Configuration
# LOG_LEVEL=info                        # Logging level (debug, info, warn, error)
# SESSION_SECRET=random_string          # Secret for session cookies (recommended for production)
# SESSION_EXPIRY=3600000                # Session expiry time in milliseconds (default: 1 hour)